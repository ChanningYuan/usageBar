# Changelog

本项目遵循 [Keep a Changelog](https://keepachangelog.com/zh-CN/) 与 [语义化版本](https://semver.org/lang/zh-CN/)。

## [0.3.36] - 2026-08-27

### 修复
- **Codex 更新后额度一直显示「连接不上 Codex」（其实不是网络问题）**：8 月 24 日随 ChatGPT.app 自动更新下发的 Codex 0.149 删掉了 usageBar 启动它时用的一个参数，Codex 一启动就拒绝退出，usageBar 误当成断网、顶着几天前的旧数字持续显示「更新于 Xd 前 · 连接不上 Codex」。现在改用新旧版本都认的参数，额度恢复实时刷新。
- **以后 Codex 再改接口会明说**：Codex 拒绝 usageBar 的启动参数时，不再冒充「连接不上」，改为提示「Codex 更新后接口有变 — 升级 usageBar 后可显示额度」，也不再拿旧数字充数。

## [0.3.35] - 2026-08-14

### 变更
- **Qoder CLI 只统计你亲手用的那部分（#9）**：只有在 Qoder CLI 交互界面里发起的对话才算 Qoder 的用量。通过 `-p`、脚本、或别的 AI 工具当后端调过来的（Agent SDK 桥接）不再计入——那类调用的用量会记在发起它的工具名下（比如 Claude Code），两边都记就重复了。
  > ⚠️ **代价**：如果你在脚本或 CI 里直接跑 `qodercli -p`，而没有别的工具在记账，这部分用量将不再出现在统计里。
- **历史数据会一并调整**：升级后首次刷新，Qoder CLI 过去按旧口径记入的这类用量会被清理，累计数字会相应减少。

## [0.3.34] - 2026-08-14

### 修复
- **Qoder CLI 的会话不再全是「(无标题会话)」（#10）**：0.3.33 接入新日志后，Qoder CLI 详情页「按会话」里每行都只有 8 位编号，看不出是哪项工作。其实这些会话在 Qoder 自己的会话列表里是有名字的，只是没被读进来。现在直接沿用 Qoder 自己的会话名（你手动改的名字 > 自动生成的标题 > 首句提问），和 Qoder 里看到的一致。
- **没有会话记录的调用也能看出是哪个项目**：带 `--no-session-persistence` 的调用（各类桥接、脚本、非交互调用）本来就不留会话记录、拿不到名字，现在退而显示它的项目目录名，不再是一串编号。

> 本次只补会话名称，**不改动任何用量数字**。

## [0.3.33] - 2026-08-14

### 新增
- **统计明细现在会存下来了**：每次扫描时把「哪个会话、用了哪个模型、各类 token 各多少」一并记录，全部 9 个数据源都覆盖。此前这些只在打开详情页时临时算一遍，原始日志被 AI 工具自己清理掉、或系统权限读不到，详情页就展不开了。

### 修复
- **Qoder CLI 的用量不再「列表有、点进去空」（#8）**：详情页过去每次都重新读一遍原始日志，日志被工具自己清理掉、或系统权限读不到时，就会显示成「该周期这个来源没有用量」，和主列表的数字对不上。现在统计明细会随扫描一并存下来，点进详情直接读它——原始日志没了也能正常展开，主列表、总量、分模型、分会话四处数字永远一致。
- **Codex 分支会话的用量不再丢失**：用 `codex fork` 分出来的会话，其用量此前不会进入统计账本，主列表少算（开发机实测某天少了 87 万 token）。
- **补上 Qoder CLI 漏统的一部分用量（#8）**：带 `--no-session-persistence` 的调用（各类桥接、脚本、非交互调用）不会留下会话记录，此前完全没被计入。现在这部分也会统计，且与原有记录按请求编号去重，不会重复计算。

### 变更
- **下架 4 个数据源**：QoderWork 与悟空（由千问办公替代）、OpenClaw 与 Hermes Agent（未使用）。主列表与设置页不再出现。
- **Qoder CLI 与千问办公的统计开关拆开**：此前「一键开启」会把两个环境变量一起写进 `~/.zshrc`、撤销时一起删；现在各产品的开关挂在各自数据源行下，各开各的、互不影响。
- **名称简化**：`Qoder (CLI)` → `Qoder CLI`、`Qoder (IDE)` → `Qoder IDE`、`Codex (OpenAI)` → `Codex`。

### ⚠️ 升级须知（历史「累计」数字会减少）
本次统计升级需要**重建一次历史索引**。重建只能扫回**现在还存在**的原始日志——那些已被各 AI 工具自己清理、轮转掉的旧日志无法恢复，对应的历史用量会从「累计」中消失（开发机实测减少约 23%，主要是 6-7 月的 Claude Code 记录）。这是一次性的，升级后新数据不受影响。

## [0.3.32] - 2026-08-12

### 优化
- Codex 重置券到期时间显示到时分（如「8-13 02:13 前有效」）——纯日期在最后一天会低估紧迫感，券实际常在深夜整点作废。

## [0.3.31] - 2026-08-12

### 修复
- **Codex 主额度池不再丢失（#7）**：额度数据源从「扫描本地会话日志」整体切换为「通过本机 Codex CLI 查询官方额度接口」（凭据由 Codex 自己管理，usageBar 不读钥匙串、不弹授权框）。超大会话日志把主池挤出扫描窗口、只剩 Spark 的问题从机制上根治；离线时保留上次数据并标注「连接不上 Codex」。

### 新增
- **Codex 额度信息更全**：主列表新增「积分」药丸（按量积分有余额或不限量时出现）与「券 ×N」限额重置券药丸；详情页新增按量积分行、重置券明细（每张各自到期时间）、企业/团队人均花费上限、支出管控与限流原因提示。
- Codex 套餐档位识别覆盖全部档位（free / plus / prolite / business / enterprise 等约 20 档，未知档位原样显示）。
- 找不到 Codex、CLI 版本过老、未登录、API Key 登录等场景各有明确提示，不再混作一句「无配额数据」。

## [0.3.30] - 2026-08-11

### 优化
- **启动不再"全程转圈等最慢的"**：数据源谁扫完谁上屏，小工具几秒内先出现，Claude/Codex 扫完自动补上；菜单栏数字随扫描进度递增，不再长时间停在「加载中」。
- **首次启动有进度了**：第一次索引历史数据时，弹层底部显示「首次索引历史数据…（x/y 数据源）」，不再只有一个干转的「刷新中…」。
- **扫描中途强退/断电不再白扫**：索引进度阶段性保存，下次启动接着上次的进度补扫，而不是从头再来。
- **「立即刷新」防连点**：刷新进行中右键菜单该项置灰——此前连点会让多轮扫描互相争抢、越点越慢。
- 日志解析加入关键字预筛，全量扫描小幅提速。

## [0.3.29] - 2026-08-11

### 修复
- **Codex 额度被新模型限额池覆盖**（[#6](https://github.com/ChanningYuan/usageBar/issues/6)）：GPT-5.3-Codex-Spark 等新模型有独立限额池，最近会话都在用它时，主套餐额度会被专属池的 0% 整个盖掉。现在分池展示：主套餐保持 `7d x%` 药丸，专属池单独一颗（如 `Spark 0%`）。兼容 Codex 客户端两代日志格式——0.147 起池的身份标签被移除，改用「窗口重置时间 + 会话模型」区分，并只显示每个池的最新窗口（手动重置后旧窗口的残留数据不再干扰）。
- **Qoder 三行额度标签与数据来源不一致**（[#4](https://github.com/ChanningYuan/usageBar/issues/4)）：QoderWork 与 Qoder IDE 可以登录不同账号，此前一律把先解出的（通常是 QoderWork）账号额度标到 CLI / Work / IDE 三行，"Qoder (IDE)" 显示的实际是 Work 账号的数据。现在双端都登录时先比对账号：同账号仍共用一份；不同账号则各行显示各自账号的额度（CLI 跟随 QoderWork）。设置页与引导文案同步加了限定条件。感谢 @EaKal-7 反馈。
- **QoderWork token 统计偶发静默归零的兜底**（[#3](https://github.com/ChanningYuan/usageBar/issues/3)）：QoderWork 启动时读取 shell 环境偶发超时，会回退到系统环境表（launchctl）——而那里的开关重启电脑就会清空，导致 token 不再落日志、统计静默归零。现在 usageBar 启动与每轮刷新都会自检，发现开关丢失就自动补写，超时回退也能接住。

## [0.3.28] - 2026-08-04

### 新增
- **开机自启**：默认开启，登录 macOS 后自动出现在菜单栏。可在 设置 → 通用 或 系统设置 → 通用 → 登录项 里关闭，两边状态互通；只在首次启动自动开启一次，关掉后不会被悄悄开回。
- **自动刷新频率可调**：设置 → 通用 新增滑轨，1–15 分钟任选（默认仍为 10 分钟），拖动即生效。

### 修复
- **国内网络「检查更新」一直转圈**：检查更新与安装包下载全面切换到 usagebar.cn（国内服务器），不再依赖对 GitHub 的直连可达性。此前不开代理基本无法完成更新检查。

### 变化
- 设置页「外观 · 主题」并入「通用」段。

## [0.3.27] - 2026-08-04

### 新增
- **千问办公（QwenWorkCN）接入**：第 13 个工具，读本地会话日志统计 token（净输入 / 输出 / 缓存读 / 缓存写四列）。它内置 Qoder 的 CN 版引擎，token 真值同样默认关闭——设置页「一键开启」现在会同时写入 `QODER_EXPOSE_TOKEN_USAGE`（Qoder CLI / Work）与 `QODERCN_EXPOSE_TOKEN_USAGE`（千问办公）两个开关，**开启后需重启千问办公才开始记录**。
- **千问办公积分**：可选开启（设置 →「账号额度与积分」，默认关，首次需一次钥匙串授权）。主列表显示「剩余 X 积分」「今日已用 X 积分」两颗药丸，详情页按今日 / 本周 / 本月等周期看消耗，并把消耗挂到具体会话上。
  - 剩余可用与官方「我的积分」页逐位一致；**不显示百分比**——官方页面没有「总额」这个概念，每日赠送额度还天天变（平时 100、搞活动 500），算出来的百分比会跳得没道理、也和官方对不上账。
  - ⚠️ 「已用」只统计真实对话消耗，**不含「每日积分过期」**。官方「已使用」列表把过期行也列在里面，所以 usageBar 的数字会比官方列表小——那部分是没花完作废的赠送额度，不是你的消耗。

### 修复
- **token 统计已开启、却一直是 0 且没有任何提示**：环境变量写进 shell 配置后，目标应用要重启才会继承。此前 usageBar 只看配置文件、认定「已开启」，于是既不给开启引导、又因为用量为 0 把整行隐藏——这个工具在界面上直接消失，且没有任何解释。现在会提示「已开启统计，重启 XX 后开始记录」；只在当前周期确实用过、却没记到 token 时才提示，当天没用过的工具不会被打扰。

### 变化
- 详情页新增声明式的「是否显示账号额度模块」开关：千问办公的额度就是一个「剩余可用」，主列表药丸已经显示过，详情页不再重复。

## [0.3.26] - 2026-07-17

### 新增
- **会话列表跟随手动改名**：在 Claude Code 里用 `/rename` 改过名的会话，会话列表现在优先显示你改的名字（此前只认自动生成的标题，改名后列表纹丝不动）。优先级：手动改名 > 自动标题 > 首句输入；复用同一列表的 Cowork / Qoder CLI / Qoder Work 一并生效。
- **账号额度历史记录**：额度数据变化时自动追加一条到 `quota-history.jsonl`（应用支持目录，纯本地文件），覆盖所有工具的额度池——信用点类（Qoder / WorkBuddy）记原始已用/总量，百分比类（Claude / Codex / Cursor）记百分比——为后续消耗分析积累数据。只在数值变化时写入，体积可忽略；界面无变化。

## [0.3.25] - 2026-07-15

### 修复
- **Qoder Teams 账号额度显示错误**：Teams 账号「用满 100%」被显示成 1%——官方接口的百分比字段在 Teams 账号是 0–1 小数、个人账号是 0–100，两种刻度混用。现改为用「已用/总量」自己计算，不再受该字段影响。
- **Claude Code 额度的重置倒计时在数据陈旧时被隐藏**：重置时间是绝对时间点，即使数据是几小时前采的、倒计时依然准确，现改为始终实时显示；已过重置点则显示「已重置」，并在额度区标注「更新于 x 前」（statusline 来源还会提示"打开 Claude 会话后自动更新"），不再无解释地置灰。

### 新增
- **Qoder Teams 显示组织资源包**：Teams 账号在席位套餐之外多显示一行「资源包」（组织共享的购买制点数池），额度一目了然。
- **额度显示 used/total 数字**：详情页额度行与主列表药丸不再只有百分比，同时显示「6,000/6,000」这样的原始数字（Qoder / WorkBuddy 信用点类额度）。
- **价格表拉取兜底**：公司安全软件按进程拦截 app 网络请求时（token 统计不受影响、但价格表停更），自动改用系统 curl 再试一次；拉取失败的具体原因会写入系统日志（Console.app 搜 `com.yuanchenyu.usageBar` 可查），便于排查。

### 变化
- **重置时间显示精简**：只有一个重置时间的工具（Qoder / Codex / WorkBuddy 等），重置时间上提到额度区头部 / 药丸行尾，只写一遍；主列表药丸的倒计时只保留最高位（如 `6d 重置` 而非 `6d22h 重置`），精确值看详情页。

## [0.3.24] - 2026-07-15

### 新增
- **账号额度显示**：主列表「今日」标签下、开启监测的工具会多显示一行额度（如 `5h 42% ｜ 3h15min 重置`、`7d 52%`），进详情页顶部还有更完整的账号额度模块（各时间窗的用量条 + 重置倒计时 + 付费档标签）。覆盖 5 个工具：Codex、Claude Code、Qoder、Cursor、WorkBuddy。额度只在「今日」标签显示——它是「此刻」的状态，不随周期变。
- **数据获取方式可选（Claude Code）**：开启 Claude 额度时可三选一——**statusline 搭便车**（推荐，往你的 statusline.sh 自动加一行、Claude 刷新状态栏就把额度写到本地文件，零钥匙串、零起进程）、**跑 /usage**（后台跑 `claude -p /usage` 读取，零钥匙串）、**联网 API**（读系统钥匙串里的登录凭证直连 api.anthropic.com，数据最全含分模型限额）。设置里可随时切换 / 重置。
- **「昨日」时间标签**：时间周期标签新增「昨日」，默认标签变为 今日 / 昨日 / 本周 / 本月 / 累计 五个（老用户若改过配置则保持原样、只多一个「昨日」候选）。

### 变化
- **额度默认开的三个是零钥匙串的**：Codex（读本地日志）、WorkBuddy（读明文登录文件）、Cursor（读明文凭证）默认开启；Claude、Qoder 涉及系统钥匙串授权，默认关，需你主动开并走一次引导。全程只在你打开弹层 / 手动刷新 / 主动开启时才会读钥匙串，app 启动本身不会弹授权框。
- **Qoder 额度自动降级**：额度是账号级的、CLI / Work / IDE 共用一份；哪个产品登录了就用哪个的凭证去查，只对用到的那个授权一次。
- **时间标签上限从 4 个提到 5 个**。

## [0.3.23] - 2026-07-14

### 修复
- **Claude Cowork、Qoder (CLI) 、Qoder (Work) 的详情页是空的**：这三个 provider 的详情页点进去只显示「该周期这个来源没有用量」，即使列表里明明有用量。v0.3.22 新增详情页时漏改了一处，本版修复。
- **会话名显示成一串编号**：WorkBuddy 和 Qoder (IDE) 的详情页里，「按会话」每行的名字是会话编号（如 `b7a01030-b21`）而不是可读的会话名。其实这两个工具的数据里都存了会话名——WorkBuddy 有它自己生成的会话标题，Qoder (IDE) 有你在侧栏看到的那个会话名——本版都取回来了。

### 变化
- **「按模型」默认只显示前 3 个**：和「按会话」一致，多出来的收进「展开全部」。此前是全部铺开，模型一多（Claude Code 累计有 7 个）整页被撑得很长。「按来源」同样处理。

## [0.3.22] - 2026-07-13

### 修复
- **Cursor 用量虚高 72%**：Cursor 的用量事件是「累计快照」——一次对话进行中会被反复上报，时间戳固定在对话开始那一刻、token 逐次累加，只有最后一份是真实值。旧版把每一份中间快照都当成新事件累加了，导致数字远超实际，而且两台电脑抄到的快照份数不同、数字永远对不上。本版改为只认每次对话的终值。**升级后 Cursor 的累计数字会变小，这是修正、不是丢数据**（本机实测：9416 万 → 5474 万，与 Cursor 官方口径一分不差）。
- **部分模型的花费算错**：`auto` 是「自动选模型」的路由名（Cursor、WorkBuddy、Qoder 都用），不是真实模型，但它会在价目表里撞上别的服务同名的模型、按人家的价格算钱——算出来的金额看着挺像回事，其实完全是错的。同类的还有 Qoder 的套餐档位名（`ultimate` / `efficient` / `lite` 等）。本版起这类通用名一律显示「无价目」，不再编造金额。

### 变化
- **详情页扩展到全部 provider**：除 OpenClaw / Hermes 外，每个 provider 都可以点进详情页看分模型、分会话。新增 Claude Cowork、Cursor、Qoder（CLI / Work / IDE）、WorkBuddy、悟空。
- **会话也显示缓存命中率**：详情页「按会话」每行新增命中率，与「按模型」同一列对齐，一眼看出哪个会话在吃缓存、哪个在烧新 token。
- **模型名原样显示**：Qoder 的 `qmodel`、WorkBuddy 的 `auto` 等厂商打码的模型名照数据原样展示，不做美化。
- **金额分三档**：能查到价的按「≈$ 等效」展示；WorkBuddy 改用它自带的「信用点（Credits）」；Qoder 全家桶因厂商未暴露真实模型名、本地也没有信用点数据，金额位显示「—」。
- **Cursor 详情页没有「按会话」**：Cursor 的本地数据里不含会话标识，拆不出会话，该区块整块不显示。
- **悟空详情页覆盖全部历史**：悟空有新旧两套用量记录，旧的那套占了总量的 99.96%。详情页把两套合并统计，因此分模型、分会话的合计与列表里的总数是对得上的。

## [0.3.21] - 2026-07-13

### 变化
- **Claude Code 合并为一行**：原「Claude Code (订阅)」「Claude Code (API)」两行合并成单个「Claude Code」。旧的订阅/API 二分并不成立——官方 API-key 直连和订阅在日志里长得一模一样，而「API」那行实际只装得下 Vertex / Bedrock；通过 ccx 等中转的流量还会被错贴成「订阅」。合并后主列表就是这份日志的真实总量，token 一个不少。
- **明细页新增「按来源」**：在 Claude Code 明细页按「官方直连 (Anthropic)」和「中转/代理」拆分 token 与等效花费，回答「为什么我的 Claude Code 里有 gpt 模型」。仅当两类都有消耗时才出现，纯官方用户看不到这个区块。

### 修复
- **价格全为 $0**：部分用户升级后所有金额显示 $0。根因是价目表拉取被网络或安全软件拦截，而本地又没有任何价目表可用。本版三处加固：安装包内置一份价目快照兜底（构建时自动从线上表生成）；本地无缓存时不再发送可能导致服务器返回「无需更新」空响应的校验头；价目表超过 7 天未更新时在偏好设置中提示。token 统计始终不受价目表影响。

## [0.3.20] - 2026-07-13

### 修复
- **明细页完整显示日志记录的模型 ID**：通过 ccx 等方式让 Claude Code 使用跨厂商模型时，日志会如实记录 `gpt-5.6-sol`，旧版却套用 Claude 友好名格式化，最终只显示成 `Gpt`。本版「按模型」列表直接展示日志原始 `modelId`，日期后缀、服务档位和代理模型名均不再丢失。

### 变化
- Claude Code、Codex、OpenCode 的模型明细统一改为准确优先的原始 ID；等效花费仍独立走远程统一价目表，显示改动不影响计价。

## [0.3.19] - 2026-07-12

### 修复
- **GPT-5.6 系列等效花费显示过低（约为真实的 1/4）**：内置价表没赶上 7 月 9 日发布的 GPT-5.6，`gpt-5.6-sol` 被按旧 GPT-5 基础价（$1.25/$10）折算，官方实际为 $5/$30。本版起该系列按官方价计算，打开明细即自动按新价重算，历史数据无需迁移。

### 变化
- **等效花费计价统一走远程价目表**：Claude / GPT 系不再使用内置价表，与其他厂商一致改用每日自动更新的远程价目表（0.3.17 引入，源自 [models.dev](https://models.dev)），新模型上市后价格自动跟进、无需升级 app。价目缓存缺失时（如首次启动）会立即拉取；极端离线场景下短暂显示 $0 并标注「无价目」，联网后自动恢复。

## [0.3.18] - 2026-07-10

### 修复
- **Codex Desktop「从其他 AI 应用导入」不再虚增当天用量**（感谢 issue 反馈）：Codex Desktop 0.142+ 的导入功能会把 Claude Code 等应用的历史会话批量生成 rollout 快照文件，其中只有 `total_tokens` 累计值、无实际输入输出。旧版把这批快照整段计入导入当天，导致列表远大于详情页（反馈实例：4.3M vs 1.8M）。本版主列表与详情页统一按「输入 + 输出」口径计量，导入快照自然归零；真实用量两口径逐事件相等，历史各天数字不变。升级后首次刷新会自动重算一次缓存。

## [0.3.17] - 2026-07-09

### 新增
- **模型价格自动更新**：新增远程价目表（服务端每日从 [models.dev](https://models.dev) 同步，覆盖约 146 家厂商 5000 个模型），app 每天检查一次（无变化零流量，有变化约下载 300KB）。glm / gemini / deepseek 等第三方模型的等效花费不再显示 $0，新模型上市**无需升级 app** 即可有价；Claude / GPT 系继续用内置精调价表。离线时用本地缓存，不影响任何功能。
- **「无价目」一键反馈**：极少数价目表也未收录的模型，明细页该行会显示「无价目」标记，点击打开预填好的 GitHub Issue 反馈，我们补录后次日自动生效。

## [0.3.16] - 2026-07-09

### 新增
- **新数据源：OpenCode**（社区贡献 [@ri-char](https://github.com/ri-char)，#2）：聚合显示 [OpenCode](https://opencode.ai) 的 token 用量，直读本地数据库、零配置。支持行内展开明细：总量 + 缓存命中环、指标区按 OpenCode 口径拆解（净输入 / 缓存读 / 缓存写 独立格，输出格内嵌「思考 (reasoning)」子项）、按模型、按会话。
  - 用量按消息实际发生的日期归因，跨天长会话不会把后续天的用量都记在创建日，「今日」所见即所得；思考（reasoning）token 计入输出，推理型模型不漏计。
  - subagent 子会话自动并入主会话，明细列表不散落子任务行。
  - 花费口径与 Claude 订阅 / Codex 一致（≈ 等效 API 价）：订阅登录 OpenCode 记 0 时按模型价目折算（GPT 系走 OpenAI 价、Claude 系走 Anthropic 价），API key 用户显示 OpenCode 记录的真实花费。
  - 图标随主题切换，深色模式用 OpenCode 官方深色变体（黑框在深色下会隐形）。

### 变化
- 明细页四格（净输入 / 输出 / 缓存读 / 缓存写）的单价改走统一跨厂商价目路由，混合厂商来源不再一律按 Claude 价折算。

## [0.3.15] - 2026-07-08

### 新增
- **点开来源看用量明细（Claude Code 订阅 / API · Codex）**：弹层里把鼠标移到这几行会浮现「›」，点进去是该来源在当前周期的详情页——大号总量 + 缓存命中环、Token 构成、按模型、按会话，每个用量数字旁并列**等效 API 花费**（订阅口径标「≈ 等效」，是「按官方 API 价值多少」的参照，非真实账单）。详情页顶部能直接切 今日 / 本周 / 本月 / 累计。
  - Claude：净输入 / 输出 / 缓存读 / 缓存写 四格 + 各模型（Opus / Sonnet / Haiku…）的命中率与花费。
  - Codex：输入 / 输出 两块，缓存输入、思考（reasoning）作为子项挂在下面；按模型区分 GPT-5.5 / GPT-5.x-Codex / o4-mini 等；会话标题取自 Codex 侧栏的真实标题。
- **外观 · 主题**：偏好设置新增 深色 / 浅色 / 跟随系统（默认跟随系统），详情页与列表整体换皮。
- **单来源直达详情**：今日只有一个可展开来源有用量时，点开菜单栏直接进它的今日详情。

### 变化
- 详情页采用跟随系统的中性灰配色，界面高度随内容自适应（内容少不留空白、内容多可滚动）。

## [0.3.14] - 2026-07-06

### 修复
- **悟空 0.9.66 起对话 token 重新计入**：悟空升级到 0.9.66 后，Codex 模式的对话改为直连远端、不再经本地中转，导致悟空用量停止增长（记录里 token 全是 0）。本版为悟空新增第二个数据源，从悟空内置 codex 的会话文件里读取真实 token，与升级前的历史数据按天相加。升级前后不重叠、不会重复计算；未使用新版的用户不受影响。

## [0.3.13] - 2026-07-02

### 新增
- **时间周期标签可配置**：弹层顶部的时间标签现在能在偏好设置里自由勾选、拖拽排序（最多 4 个）。新增 **本周 / 本月** 日历对齐周期（本周可选 周一 / 周日 起始），还能加一个 **自定义日期区间**。默认标签改为 **今日 / 本周 / 本月 / 累计**。
- **数据源带图标**：偏好设置的数据源列表每行加上了对应工具的图标，更好认。

### 变化
- **偏好设置改版**：数据源、时间周期标签拆成两个可折叠区块，更清爽（全内联、无弹层焦点问题）。
- **菜单栏固定「今日」**：菜单栏数字始终显示今日用量，切换弹层里的标签不再影响它；下次打开弹层默认回今日。
- 弹层只剩「今日」一个标签时，顶部改为标题 + 「＋自定义」入口（点击直达偏好设置的时间周期标签，自动展开定位）。

### 修复
- 修复**首次打开弹层时与菜单栏之间出现间隔**的问题（弹层尺寸首开未按内容自适应）。

## [0.3.12] - 2026-07-02

### 修复
- **悟空缓存命中真正生效**：v0.3.11 号称补了悟空缓存命中，但那次构建没把改动编进发布包，升级后悟空命中率仍是空的。本版重新干净构建、并强制缓存重扫，悟空的「xx.x% 缓存命中」+ 悬浮拆分现在真的会显示了。总用量数字不变。

## [0.3.11] - 2026-07-02

### 修复
- **悟空缓存命中补显**：悟空（钉钉）在 v0.3.10 的「缓存命中率」里一直显示 0%——它的 `cacheTokens` 字段是数据源 2026-05-18 才新增的，之前的解析没读它。现已补上：悟空行也能看到缓存命中占比与「缓存 / 非缓存」悬浮拆分了。**总用量数字不变**（缓存命中本就含在输入里，只是把其中的命中部分标出来），2026-05-18 之前的老记录无此字段、按无缓存显示。

## [0.3.10] - 2026-07-02

### 新增
- **缓存命中率**：各工具行显示「xx.x% 缓存命中」，鼠标悬浮可看「缓存 / 非缓存」的绝对值拆分——一眼知道用量里多少是计费打折的缓存命中读取。
- **用量数字点击复制**：数字带虚线下划，悬浮看精确值、点击即复制到剪贴板（「合计」同样支持）。

### 变更
- **界面重构**：进度条移到工具名下方、更细，单色纯表用量对比；数字移到右侧整行居中；字号统一、整体更紧凑。
- **切换周期时菜单栏同步**：在弹层切「今日 / 7天 / 30天 / 累计」时，菜单栏标题数字实时跟随，不再和弹层对不上。

### 修复
- **Cursor 用量少算修正**：Cursor 之前只统计输入/输出、漏掉了缓存两桶，用量被低估（约 87%）。现已补回——**Cursor 数字会一次性上升，这是补回缓存、非重复计数**。

## [0.3.9] - 2026-06-30

### 新增
- **数字悬浮看精确值 + 点击复制**：鼠标停在「合计」或某工具的用量数字上，即时浮出精确值（带千分位）；点一下即把整数复制到剪贴板，方便贴到表格/记账。
- **空状态引导**：某个时间视图下完全没有用量时，显示一句引导（如「今天还没烧 token —— 快去蹬两下 AI 🚀」）。

### 变更
- **各视图只显示「该周期用过」的工具**：今日 / 近 7 天 / 近 30 天 / 累计，每个视图只列在该周期有用量的 provider，没用量的自动隐藏，列表更清爽。（Qoder CLI/Work 装了但还没开 token 统计的仍保留并挂「去开启」引导，不会被隐藏。）
- **provider 按用量从高到低排序**：用得多的排在上面。

## [0.3.8] - 2026-06-26

### 修复
- **修复 Codex「fork」会话 token 重复计数**：用 `codex fork` 从已有会话分叉时，新会话文件会把父会话的整段 token 历史一并 replay 进去；旧版逐文件独立统计会把这段重复计入，导致 Codex 用量虚高（实测可达 2~2.5 倍）。现已按会话的 `forked_from_id` 识别 fork 文件、以父会话最终值作差分基线去重，只统计 fork 后的真实新增。
  没用过 `codex fork` 的用户数字不变；交互式 / `codex exec resume` 不受影响（它们只追加回原会话文件、本就不会重复计）；子代理（subagent）会话独立计账、不参与去重。

## [0.3.7] - 2026-06-25

### ⚠️ 重要变更（升级后你的 Claude 数字会变大）
- **补回 Claude 子代理 / workflow 用量**：之前「Claude Code（订阅 / API）」的统计漏掉了所有**子代理（Task）**和 **ultracode workflow** 跑出来的 token——它们的 transcript 存在各会话的 `subagents/` 子目录里，旧版扫描时被整体跳过、从未计入。现已纳入。
  **升级后你的 Claude 数字会上升**，跑过 workflow / 大量子代理的用户升幅明显——这是补回此前漏算、**不是重复计数**：这部分用量独立于主对话（`message.id` 不与主对话重叠），不会和已有数字相加成双。

## [0.3.6] - 2026-06-25

### 新增
- **「Qoder (Work)」用量统计一键开启**：QoderWork 和 Qoder CLI 一样，默认不把 token 用量写到本地（显示 0）。现在偏好设置的 Qoder 区**顶部有一个共享开关**，一键开启后 **Qoder CLI 和 QoderWork 同时生效**（写入环境变量 `QODER_EXPOSE_TOKEN_USAGE=1`，不动你 `~/.zshrc` 里的其它内容）；首次开启后 CLI 新开终端、QoderWork 重启 app 才开始记录，可随时一键撤销。菜单弹层也会在未开启时于对应行下提示「去开启」。（Qoder IDE 不受此限制，本就正常统计。）

### 修复
- **首次运行不再误隐藏「Qoder (Work)」**：用过 QoderWork 但还没开启用量统计（本地全 0）的用户，首次运行不会再把这一行自动隐藏，确保能看到开启引导。

## [0.3.5] - 2026-06-24

### 修复
- **修复「Qoder (Work)」统计归零**：QoderWork 桌面 app 升级到 0.6.3 后改了 token 数据的存放位置，导致这一行从某天起一直显示 0。现已接入新数据源，并与历史数据相加，「Qoder (Work)」恢复正常统计、历史数字不丢。

## [0.3.4] - 2026-06-24

### 变更
- **DMG 安装窗口加拖拽引导**：安装窗口新增一条从应用图标指向「应用程序」文件夹的虚线引导箭头，底部加上中英双语「拖拽到右侧安装」提示；应用图标加了柔和投影，在白色背景上更清晰、更易看出是一张可拖拽的卡片。

## [0.3.3] - 2026-06-24

### 新增
- **Qoder (CLI) 用量统计一键开启**：新版 qodercli 默认不把 token 用量写到本地，导致「Qoder (CLI)」一直显示 0。现在偏好设置的 Qoder (CLI) 下会引导一键开启（写入环境变量 `QODER_EXPOSE_TOKEN_USAGE=1`，不会动你 `~/.zshrc` 里的其它内容），开启后**新开终端**跑 qodercli 即可正常统计；可随时一键撤销。菜单弹层也会在未开启时提示「去开启」。

### 修复
- **首次运行不再误隐藏 Qoder (CLI)**：用过 qodercli 但还没开启用量统计（本地全 0）的用户，首次运行不会再把「Qoder (CLI)」这一行自动隐藏，确保能看到开启引导。

## [0.3.2] - 2026-06-17

### 新增
- **首次运行智能默认**：第一次拉到数据后，只保留有用量的 provider，零用量的自动隐藏（仅一次，不覆盖你之后的手动开关）。新用户不再一打开就是一长串空行。

### 变更
- **DMG 安装窗口美化**：图标布局/窗口大小调整，更接近常规 app 的拖拽安装界面。

## [0.3.1] - 2026-06-17

### 变更
- 自动更新弹窗等 Sparkle 界面现在**跟随系统语言**显示中文（之前固定英文）。
- 偏好设置标题栏显示**当前版本号**。

## [0.3.0] - 2026-06-17

### ⚠️ 重要变更（升级后你的数字会变化）
- **修正 Claude token 重复计数**：同一条 API 响应在流式落盘时会被写多行、usage 完全相同，旧版逐行累加导致 Claude（订阅 / API）数字虚高约 2 倍。现按 `message.id` 去重，只计一次。
  **升级后你的 Claude 数字会一次性下降约一半——这是修正虚高、不是数据丢失**，历史趋势图会出现一个明显台阶。

### 新增
- **Claude Cowork 用量统计**：扫描桌面端 Claude（`~/.claude/local-agent-mode-sessions/`）的 transcript，归入 Claude 组。
- **自动更新弹窗显示更新说明**：点"检查更新…"时直接看到这一版改了什么（自动取自本 CHANGELOG）。

### 变更
- 偏好设置里开关的开启态改为系统蓝，明暗主题下都清晰。

## [0.2.0] - 2026-06-10

### 新增
- **应用内自动更新**（基于 Sparkle）：右键菜单"检查更新…"，新版会自动提示下载安装。
  - ℹ️ 从此版起支持自动更新；早于 0.2.0 的版本需手动下载一次本版，之后即可自动更新。
- **DMG 拖拽安装包**：Releases 新增 `usageBar.dmg`，双击拖进 Applications 即可。

### 变更
- 发布版（.app / .dmg）均已 **Apple Developer ID 签名 + 公证**，双击即开，无需手动放行。

## [0.1.0] - 2026-06-04

### 新增
- 首个公开版本。macOS 菜单栏聚合显示多个 AI 编程工具的 token 用量。
- 支持 Provider：Claude Code（订阅 / API）、Qoder（CLI / Work / IDE）、Codex、悟空、WorkBuddy、Cursor、OpenClaw、Hermes。
- 今日 / 近 7 天 / 近 30 天 / 累计四视图，偏好设置可自定义各 provider 可见性。
- 全本地直读（jsonl / SQLite），零配置（Cursor 除外，需联网）。
- 安装方式：人类下载 .app / AI Agent 走 install-usagebar skill 一句话装。

[0.3.6]: https://github.com/ChanningYuan/usageBar/releases/tag/v0.3.6
[0.3.5]: https://github.com/ChanningYuan/usageBar/releases/tag/v0.3.5
[0.3.4]: https://github.com/ChanningYuan/usageBar/releases/tag/v0.3.4
[0.3.3]: https://github.com/ChanningYuan/usageBar/releases/tag/v0.3.3
[0.3.2]: https://github.com/ChanningYuan/usageBar/releases/tag/v0.3.2
[0.3.1]: https://github.com/ChanningYuan/usageBar/releases/tag/v0.3.1
[0.3.0]: https://github.com/ChanningYuan/usageBar/releases/tag/v0.3.0
[0.2.0]: https://github.com/ChanningYuan/usageBar/releases/tag/v0.2.0
[0.1.0]: https://github.com/ChanningYuan/usageBar/releases/tag/v0.1.0
