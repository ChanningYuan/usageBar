# ── usageBar 公开仓库 · 白名单 ──
# 默认忽略仓库内所有内容,只放开发布所需的目录/文件。
# 内部研发笔记(docs/、CHANGELOG.md、CLAUDE.md、AGENT.md、extras/ 等)
# 保留在本地工作目录,但不进公开仓库。

/*

!/app/
!/icon/
!/assets/
!/README.md
!/LICENSE
!/CHANGELOG.md
!/appcast.xml
!/.gitignore

# ── 放开目录内仍需忽略的构建产物 ──
app/.build/
app/.swiftpm/
app/Packages/
app/dist/
app/release-escrow/   # 应急发版（GitHub 不可用）时保管 zip/dmg 的目录，不进仓库；不能放 dist/ 下（build-app.sh 会清空）
app/*.app/
app/DerivedData/
app/xcuserdata/
**/.DS_Store
**/*.swp

# 指向知识库文档（个人规划/调研/内部changelog），不进 git
_notes
